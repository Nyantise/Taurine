import { observable } from "@legendapp/state"

export const themeController = {
    state: observable({
        window:{
            borderRadius:12
        }

    }),

    loadTheme: async () => {

    },
}